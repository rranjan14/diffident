[toolchain]
# gpui on zed main uses stdlib features stabilized after 1.91 (e.g. slice_as_array).
channel = "1.97.1"
# clippy: CI lints with -D warnings. llvm-tools: cargo-llvm-cov needs it for coverage.
components = ["clippy", "llvm-tools"]
